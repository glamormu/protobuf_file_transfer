INCLUDE(FindProtobuf)
FIND_PACKAGE(Protobuf REQUIRED)
INCLUDE_DIRECTORIES(${PROTOBUF_INCLUDE_DIR})
PROTOBUF_GENERATE_CPP(PROTO_SRC PROTO_HEADER file_transfer.proto)
ADD_LIBRARY(proto ${PROTO_HEADER} ${PROTO_SRC} ../include/sec_variant.hpp)
